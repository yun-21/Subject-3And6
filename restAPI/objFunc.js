/**
 * 
 * @param {객체함수의 번호 값} id 
 * @param {객체함수의 제목 값} title 
 * @param {객체함수의 내용 값} body 
 * @returns 
 */
const func = (id, title, body) => {
    return {
        번호: id,
        제목: title,
        내용: body
    }
}